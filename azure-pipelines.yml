pr: none

trigger:
  branches:
    include:
    - '*'

jobs:
  - job: Build
    timeoutInMinutes: 8
    cancelTimeoutInMinutes: 2
    pool:
      vmImage: 'Ubuntu-16.04'
    steps:
      - template: azure-pipelines-steps.yml
