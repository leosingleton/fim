{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "build",

    // Suppress TypeScript type declarations as these JS bundles are consumed directly by the HTML UI
    "declaration": false,

    // Support jQuery
    "esModuleInterop": true
  },
  "exclude": ["**/__tests__/*", "**/build/*"]
}
